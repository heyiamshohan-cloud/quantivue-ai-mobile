# Quantivue AI Mobile — artifact status

No APK/AAB was generated in the Arena agent container. The audited environment has no `java`, `gradle`, Android SDK, `adb`, or Android build tools. This file is a status record, not a binary artifact. Run `scripts/build_debug.sh` or `scripts/build_release.sh` in a configured Android toolchain; the scripts copy only real artifacts and then write hashes.
